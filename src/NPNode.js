import State from './state'

class NPNode {

	type = 'NP'
	errors = ['This noun phrase is all alone. It must be part of a clause.','This noun phrase does not correctly generate text.']
}

export default NPNode
